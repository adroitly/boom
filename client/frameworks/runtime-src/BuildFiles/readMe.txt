————————————————readMe.txt————————————————

copy-config.json配置详情

folder          : "需要拷贝的文件夹"

files           : "需要拷贝的文件"

source          : “资源文件夹或资源文件”

targetFolder    : “目标文件夹，默认为工程根目录”

isNew           : “是否新建，默认为false”

isFilter        : “是否过滤，默认为false，开启这个请检查copy-filter.json是否有想要的工程目录”



copy-filter.json配置详情

filterProject   : “project的路径(全路径)”



version.json详情

app_version     : “出包的app_version”
app_versionCode : “出包的app_versionCode”


